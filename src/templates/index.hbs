<!DOCTYPE html>
<html lang="en">

<head>
	<title>menosketiago™ — Product Design, Design Systems and Design Ops</title>
	
	{{> head/meta}}
	{{> head/og}}
	{{> head/links-scripts}}

	<link rel="me" href="https://mastodon.social/@menosketiago">
</head>

<body>
	<div class="wrapper">
		<header>
			{{> layout/header}}
		</header>

		<main>
			{{!-- Content sections --}}
			{{> sections/splash}}
			{{> sections/about}}
			{{> sections/work}}
			{{> sections/content}}
			{{> sections/contact}}
			{{> sections/bye}}
		</main>

		{{!-- Back to top button --}}
		{{> back-to-top}}

		<footer>
			{{> layout/footer}}
		</footer>
	</div>

	{{> tooltip}}

	<div class="cursor"></div>

	{{> scripts}}
</body>

</html>