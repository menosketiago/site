<section id="work">
    {{#work}}
        {{#each pages}}
            {{!-- Work items wrapper (one for each 9 items) --}}
            <div id="page-{{@key}}" class="wrapper">
                {{#each page}}
                    <a id="{{name}}" class="item {{#if extra}}{{extra}}{{/if}}" tabindex="0" {{#if dark-mode}}data-dark{{/if}}>
                        <h3>{{category}}</h3>
                        <h2>{{title}}</h2>
                        <div class="view">
                            {{> icons/view}}
                            View
                        </div>
                    </a>
                {{/each}}
            </div>
        {{/each}}
    {{/work}}

    {{!-- Buttons to scroll work items sideways --}}
    <button class="scroll left is-hidden" tabindex="-1">{{> icons/chevron}}</button>
    <button class="scroll right is-hidden" tabindex="-1">{{> icons/chevron}}</button>
</section>